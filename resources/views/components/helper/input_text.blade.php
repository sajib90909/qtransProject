<div class="form-group">
    <input type="{{ $type ?? 'text' }}"
           name="{{ $name ?? '' }}"
           class="form-control"
           id="{{ $name ?? '' }}"
           required
           placeholder="{{ $placeholder ?? '' }}">
</div>
